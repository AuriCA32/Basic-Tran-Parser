
with
    var x <- 4, y<-9 : int
    var m, p <- 'p' : char
    var a : int
begin
    with
        var s: int
    begin
        with
            var q : int
        begin
            a<-x+y;
        end    
        a<-x+y;
    end
end