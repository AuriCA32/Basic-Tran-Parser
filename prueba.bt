
with
    var x <- 4+(6*2), y<-9 : int
    var p : bool
begin
    x<-4;
    y<-6/2;
    p <- true /\ true \/ false;
    p <- 2 <= 3;
    with 
        var x <- true : bool
        var x : int
    begin
        p <- x;
    end
end