with
    var a <- true,b <- false,c : bool
    var x, y : int
    var z <- 'z' : char
    var m : array [3] of int
begin
	a <- a::b;
	a <- $a;
    for i from 1 to 4 ->
        a <- a[0] + b[1];
    end
end