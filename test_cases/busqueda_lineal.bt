with 
	var foo : array [42] of int
	var value <- 0: int
	var my_boolean <- false: bool
begin
    for i from 0 to 42 ->
    	if value = foo[i] ->
    		my_boolean <- true;
    	end
    end
    print(my_boolean);
end
